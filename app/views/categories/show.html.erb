<h1><%= @category.name %></h1>

<!-- Turbo frame to render the list of todos -->
<%= turbo_frame_tag "todos" do %>
  <%= render @todos %>
<% end %>

<!-- Turbo frame for adding a new todo -->
<%= turbo_frame_tag "new_todo" do %>
  <%= render "todos/form", todo: @todo %>
<% end %>
